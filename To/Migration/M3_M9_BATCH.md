# 第3～9轮连续迁移执行单

用户授权：第3～9轮连续实现，最终统一交付整体测试；中间不要求逐轮人工审核。
不生成Windows测试包，不新增玩法，不修改冻结Godot源快照。

## 范围与完成门槛

|阶段|内容|当前状态|
|---|---|---|
|3|章节12波、续战、节点继承、重试与复活|运行快照、节点检查点、死盘/耐久失败、重试复活与结算数值已接入并回归；三选一/结算正式界面、完整磁盘恢复仍未完成|
|4|能量、奖励、水晶卡、印记、技能道具|能量与水晶升级核心、12张卡原图和文案已导入；选择界面、技能和战斗接入未完成|
|5|首局教学、阶段引导、Z流程|待迁移|
|6|正式HUD、大厅、结算及全部有效弹窗|节点完成弹窗已按原素材接入；其余正式界面待迁移|
|7|局外进度、交易模拟、存档与恢复|任务/签到/购买/碎片核心及原子存档已验证；大厅、完整运行存档接入未完成|
|8|全局层级、比例适配、安全区与UI截图|待迁移|
|9|完整规则回归、异常恢复、审核/模拟工具|新增核心与既有战斗回归通过；全流程和全UI验收未完成|

源事实以冻结Source中的运行代码为准；文档中旧两次点击、旧元素排队和旧教学奖励描述不得覆盖后续实装规则。
当前M2WaveBattle仅是旧20波战斗小样，不冒充章节完整版。
每个模块必须有源入口、Unity入口、测试证据以及明确未验证项，才可更新状态。
第10轮Android真机性能和最终真人验收不在这次3～9实现范围内。

## 2026-09-11 基础批次实装与证据

正式工程：`D:/UGit/T0/To`。这些改动尚未形成3～9轮整体测试版，不应把现有M2场景当作完整迁移结果。

- `CampaignFlow.cs`：冻结章节原数据12+20波；脚本教学门、批次刷怪、Boss出场延迟、清波门及恢复时重置批次节奏。未接正式战斗场景。
- `SkillEnergy.cs`：满能量等待光点结束、单待用技能槽、消耗后释放下一次选择请求。未接HUD和技能执行。
- `CrystalProgress.cs`：冻结源数据驱动的升级/叠层/间隔/伤害和状态恢复。保留源代码“额外目标数仅保存未执行”的行为，不自行加攻击。
- `MetaProgress.cs`：每日任务和活跃度、七日签到、回拨日期防重复、购买限额、旧权益ID归一、碎片升星及满级转币。真实金额入口只允许显式注入本地模拟器，无支付SDK。
- `BoardModel.Capture/Restore`与`BattleMonster.Capture/Restore`：棋盘分数/最高等级、怪物血量/路径/状态层和毒tick计时；灼烧飘字累计按Godot恢复为零。
- `ProfileStore.cs`：独立Unity存档封套、SHA256校验、临时文件落盘、原子替换、备份回退；坏档不会静默当作新档。不读取或改写Godot用户存档。
- `Resources/Campaign`：32波JSON、卡片/水晶数值JSON、12张原卡图和稳定meta。素材由隔离Godot工作副本导出，不改冻结Source。

验证入口：`Migration/Tools/ValidateM2Editor.ps1`；最终基础证据：`Migration/Reports/M3M9/Foundation06/`。

- `campaign-core.txt`：新增核心全部通过，含32波终止遍历、卡片资源存在性和损坏存档回退。
- `editor-result.txt`：原有战斗播放回归PASS，95256次断言（包含逐帧重复断言，不代表95256个独立用例）。
- 无Windows构建。隔离验证工程运行，不关闭或覆盖用户打开的Unity场景。
- `editor.log`仍有UnityEditor.Search.SearchDatabase启动索引异常；该堆栈不在游戏代码中，验证成功，但不能称为编辑器零异常。

## 继续接入顺序（不需要重新逐轮授权）

1. 正式章节运行控制器接入M2战斗：Boss表现、死亡结算、节点检查点、重试和复活。
2. 能量光点/三选一/水晶强化与六印记、两个即时道具，奖励提交防重入。
3. 按原场景移植教学、阶段提示、Z流程及正式大厅/HUD/全部有效弹窗，不能以调试OnGUI替代。
4. 把局外核心与完整运行快照接入统一存档；恢复未完成的奖励门与章节完成分支。
5. 全UI适配、层级、安全区、截图审核，再做全流程和长时间回归后交付整体测试。

## 章节联调接入（后续批次）

Unity菜单 `Merge To 10/Open Chapter Integration` 创建/打开独立联调场景，保留旧M2测试场景。默认从非教学首波（索引1）开始，Inspector可指定1～31波索引。

- `M2BattleDemo.CampaignMode`使用32波配置，而不是原20波测试平衡；同一时刻只运行一种刷怪系统。
- 合成结算冻结章节刷怪计时；清波后等待推进，运行中和重复推进请求均拒绝。
- 小Boss120HP、最终Boss300HP，出场延迟及速度/耐久伤害按源配置；原哥布林/僵尸图片与80×scale尺寸独立于普通史莱姆。
- **这是联调入口，不是正式UI**。调试继续按钮不会发奖励，不能据此测试章节奖励收益。教学、节点奖励弹窗、结算、检查点、重试和复活尚未完成。
- `ChapterIntegration01`与Boss图片修订后的`ChapterIntegration02`运行回归均PASS，包含真实战斗Boss血量、冻结计时、推进防重复、切回旧模式重置。后者110893次断言，含逐帧重复检查，未打Windows包。

## 章节恢复、奖励与节点正式视图批次

以下更新替代上节“奖励未接入/可以调试跳过”的旧状态，但不代表全流程完成：

- `M2BattleDemo.CaptureCampaign/RestoreCampaign`保存恢复棋盘、活怪物及状态、波次、水晶强化、击杀/漏怪、耐久和时间。恢复时取消旧飞行弹道；未接自动磁盘保存与完整用户进度。
- 节点进入时记录独立检查点；失败后`RetryChapter`恢复检查点；`ReviveChapter`恢复失败快照、回满20耐久、清除最低等级方块，等待0.18秒后按已有下落补齐流程恢复输入。拒绝重复提交。
- 水晶奖励生成三个不同有效卡ID，限制从当前候选领取一次；领取前不能推进。强化和已领标记可随运行快照恢复。三选一正式界面尚未接入，因此联调场景在该处会等待，不应期待调试继续跳过。
- 水晶主击伤害、攻速、已安装元素及穿透伤害逻辑接入现有攻击链；附加弹道/后坐力等表现仍需逐项对齐。
- `RunSettlement`按Godot公式计算胜败基础金币、双倍金币与水晶；战斗失败/完整续战结束时提交到内存Meta一次。结算正式界面和整个Meta磁盘恢复未接入。
- `ChapterNodeView`正式节点完成弹窗使用原四张PNG、设计坐标、12px九宫格、0.22秒入场缩放/0.16秒淡入、按钮0.96倍按压。通过`GateChanged`接入，不用OnGUI绘制此弹窗。
- 使用Noto Sans SC 700/900子集覆盖节点文案；与Godot优先选择的Microsoft YaHei UI存在字形差异，仍需人工审核。当前导出的版权/授权声明保留在Campaign资源中，完整字体许可证应在分发审核中再核对。
- 已有节点Canvas屏幕比例适配和输入穿透拦截，但尚未完成全局安全区、多比例、多弹窗与触摸真机回归。

验证记录：`ChapterRecovery01`至`ChapterRecovery04`全部PASS；`ChapterRecovery04`90572次断言（含逐帧重复项）。`ChapterNodeUI01`98890次断言PASS，包含真实清波显示、按钮推进、重复提交拒绝。最终`ChapterNodeUI02`88789次断言PASS，`chapter-node.png`已人工可视检查：节点标题、说明、原图与按钮均显示，没有缺字或裁切；字体细节和整体风格仍留待统一人工审核。

本批没有打Windows包，没有实现教学或六印记，也没有把M2调试面板作为正式HUD交付。

## 2026-09-11 六印记、正式选择界面与磁盘存档接入

本节替代前文对应模块的旧状态。**仍不是教学→大厅→战斗→结算的完整交付版**，不能将核心验证通过写成全局UI/适配已完成。

### 已实现

- 六印记规则接入真实合成收尾：锤子、数盘、魔箱、晶模、魔炮、投石。单个待用槽；整批连锁及攻击完成后触发一次。魔炮与投石从合成位置发射。核心六种规则均有验证，实际战斗覆盖锤子及随机印记；全部六种逐一真人手感验收尚未完成。
- 能量来源接入：击杀5点，超过合成前棋盘实际最高数字的结果按6×(消耗方块数)计入；不是每次普通合成都加能量。原HUD素材、能量光点、视觉数值追赶、满能量0.24秒等待和准备印记槽已接入。
- 待用印记从HUD飞至合成位置，飞行0.32秒、消散0.18秒后执行技能。当前尾迹用UI圆点绘制，未达到Godot原Line2D渐变尾迹、爆点与加色混合的完全表现一致；仍需补齐视觉细节。
- ImprintChoiceView使用原蓝色卡框、原图标、文案与设计坐标；三张不同印记，确认后飞入HUD并写入单待用槽。源代码的两个广告按钮仅是占位，三张卡均可选，不接广告支付。
- CrystalChoiceView使用原金色卡框、翻牌、星级、新卡角标与确认飞行。源卡背路径缺失，按原运行时空纹理行为导出透明占位，不发明替代卡背。Boss节点顺序修为节点提示→水晶选择→满能量印记选择→下一波。
- 领取记录写入永久卡等级、已见标记及本局卡组顺序/次数/等级；合成次数和最大单次合成加入运行快照。结算快照保留本局卡组，但正式结算视图未接。
- CampaignPersistence已加入 `Merge To 10/Open Chapter Integration` 菜单的场景升级列表。用户需通过该菜单重新打开一次，以将组件加到现有章节场景；不替换用户当前未保存场景。
- 存档默认 `Application.persistentDataPath/unity_campaign_v1.json`，与Godot存档隔离。包含Meta、当前章节、棋盘、怪物状态、能量和待用印记、水晶强化、节点检查点、失败快照、奖励已领标记、本局统计。稳定边界自动保存、2秒检查、失焦/退出保存；合成中保留上一个稳定快照。
- 坏档加载失败时保留原文件并暂停；当前错误仅进入Console，正式错误弹窗未接。恢复未完成教学、教程完成标志、大厅导航、即时道具状态不在现有快照内，不能称为完整产品存档。

### 验证证据

- CrystalChoiceUI03：106707次断言PASS（含逐帧重复检查），水晶UI与完整会话往返。
- ImprintUI02：93577次断言PASS，包含光点到达后弹窗、第三张卡无广告可选、重复确认防重和待用印记恢复；imprint-choice.png已目视确认素材与文案未裁切。
- 最终 ImprintUI05：117058次断言PASS（含逐帧重复检查），新增HUD飞行、实际磁盘保存组件、本局卡组保存、存档恢复后真实合成和一次消耗验证。正式工程与隔离工程的全部C#及shader文件哈希一致。ImprintUI01编译缺少命名空间、ImprintUI03测试早于HUD初始化的失败已分别修复，不能把这些失败报告当最终结果。
- 字体已扩充为冻结脚本全部文案字符，使用Noto Sans SC 700/900；与原Microsoft YaHei UI仍有差异。细描边、卡片选中轮廓、HUD圆角及加色光效还需美术/多分辨率审查。
- 本轮未生成Windows包，也未关闭用户Unity。

### 剩余完整接入项

1. 实际教学控制器及教学演出，正式新局棋盘生成（当前仍是联调固定棋盘）。
2. 即时道具执行与正式交互、全部六印记表现逐项对照。
3. 正式胜败结算、章节完成/续战分支、Z恭喜界面与大厅导航闭环。
4. 大厅HUD和其局外入口、正式战斗顶部HUD、移除联调面板。
5. 上述状态全部落盘及异常恢复；全UI安全区、异形屏、多比例、弹窗优先级、触摸联调。
6. 全流程运行及真人手感验收。现有Canvas比例缩放不等于适配验收完成。
