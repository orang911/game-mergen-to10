# M1 棋盘迁移：可运行初版，尚未完成等价验收

## 最新：v02 已通过技术回归

统一阴影、原版合成残影、入场错峰时序与底板裁切已补齐；14,079 项断言、三种比例和默认图形后端的实际运行通过。完整结果、截图及仍有差异的项目见 [M1_V02_REPORT.md](M1_V02_REPORT.md)。最新程序位于 Builds/M1V02，旧版 Builds/M1 保留。以下 v01 记录为历史依据，不代表最新待办。

M0 已于 2026-09-11 获用户确认。目标 Unity 6000.3.11f1，原 Godot 工程和冻结 Source 未改动。

## 已实现

- 独立 BoardModel、原版 double 精度补块策略；手动 DFS、自动 BFS。
- 5×5 世界空间 SpriteRenderer 棋盘，原版背景/背板/字形图集直接复用。
- 941×1672 等比坐标转换、释放时点击、输入结算锁。
- 合成吸收、下落补块、最多五步结算、连续合成背板高亮、弹出和序列帧、震屏。
- 中途重置取消动画；测试场景显式标注无战斗，不伪造完整流程。
- 隔离 Unity 编译工具和实际播放器截图冒烟测试。

## 已验证

C# 纯规则与冻结 Godot oracle 对照：288 组、13,280 项断言通过。
包括补块权重、评分和 2,592 次指定随机数抽样。并不表示 Unity 场景或手感验收通过。

Unity 6000.3.11f1 内部测试及 Windows 开发构建通过：13,677 项断言。
最终构建日志：Validation/m1-build-peak.log；报告：../Builds/M1/verification.txt。

实际 Windows 播放器在 470×836、360×800、384×512 三种窗口比例通过合成、自动连锁、25 格结算、最终缩放恢复、中途重置测试。
高亮峰值同帧开关对照的有效变化像素分别为 3,586、2,131、1,349；阈值为每像素 RGB 总差 >6，要求至少 32 像素变化。
图片和报告在 ../Builds/M1/Verified对应分辨率/。截图采用 URP 离屏相机请求，不包含临时 IMGUI 面板。
此前 Runtime470x836 和 RuntimeD3D11 的隐藏窗口截屏为黑帧，只能证明逻辑运行，不作为视觉证据。

正式 Assets 中代码、着色器和资源与最终测试副本哈希一致；正式场景已绑定正式工程已有脚本 GUID，未覆盖已有 GUID。
检查了初始棋盘、峰值截图和 3:4 布局，但尚未做与 Godot 的逐像素色彩或真人手感验收。

## 尚未验收/待补齐

- 原版合成残影材质和统一棋盘阴影层已在 v02 迁移并验证；边缘、过滤和透明混合的逐像素差异仍待最终验收。
- 初次入场逐格下落已实现；失焦/多点触控取消已做基础处理，仍待真机及系统切换验收。
- 屏幕色彩、声画时序、接缝、极限大组逐帧对照。
- Z 完成的正式界面/消除演出由完整流程阶段衔接。
- M2 战斗冻结/批次消费仍未迁移；当前 BatchCompleted 只是接入点。
- M3 正式 Canvas UI、教学、局外与存档；M4 真机性能和用户手感验收。

## 入口

场景计划入口：Assets/MergeTo10/Scenes/M1BoardParity.unity。
验证副本：Migration/Validation/M1Project（不作为正式工程使用）。
测试按钮是临时诊断面板，不是正式 UI 美术替换。

打开正式工程中的 M1BoardParity 场景并按 Play，棋盘在运行时创建。点击相邻同级数字发起合成；Reset board 重置；Run merge 运行固定起手。
Windows 程序：../Builds/M1/Windows/MergeTo10M1.exe，需保留同目录的数据与运行库。
原版白色方框式合成序列帧及宽屏左侧灰边均按冻结资源/布局保留，本轮未改美术和已登记缺陷。
