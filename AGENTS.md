# 《晶核守卫》项目执行规则

## 适用范围与事实优先级

本文件适用于 `game_mergenTo10_published` 及其全部子目录。

所有美术生产、效果图、高清重置、切图、抠图、资源入库和运行时替换任务，开始前必须完整阅读：

`docs/27_art_production_and_asset_pipeline_standard_2026-08-06.md`

冲突时按以下顺序裁决：

1. 用户本轮明确要求；
2. 最新确认的效果图或实机截图；
3. `docs/27_art_production_and_asset_pipeline_standard_2026-08-06.md`；
4. 其他当前有效文档；
5. 历史旧图与归档资料。

运行时事实、数值和配置以代码及实际资源为准。产品边界另以 `docs/00_current_product_baseline.md` 为当前基线。

## 开工前强制分类

先将任务归为以下一种，不得混淆：

- 新方案：允许改变构图，但内容和功能不变。
- 风格迁移：锁定布局，只替换视觉语言。
- 精确编辑：只修改用户点名区域。
- 高清重置：只提高分辨率、边缘、材质和过渡，不重设计。
- 独立素材生成：输出便于抠图或透明的单体资源。
- 切图：效果图已确认，不再设计，只做正式资源拆分。
- 动效：锁定主体、画布、锚点和基线，只设计时间变化。

每张参考图必须标明角色：`LAYOUT_REFERENCE`、`STYLE_REFERENCE`、`CONTENT_REFERENCE` 或 `EDIT_TARGET`。风格参考不能改变布局；编辑目标中未点名部分必须保持不变。

## 不可擅自修改项

用户未明确授权时，必须锁定：

- 竖屏比例、安全区和功能布局；
- 怪物路径的起点、终点、转弯、方向、宽度和闭环状态；
- 魔法门朝向；
- 水晶塔位置及其与棋盘的关系；
- 5×5 棋盘的位置、尺寸和格数；
- 顶部 HUD、底部能量区、道具区和按钮槽位；
- 文案、颜色身份、主体数量、功能和状态。

局部反馈只授权局部修改。一次修订只处理一个主要变量，不得借机重做整张图。

## 视觉标准

- 主题：魔法水晶 × 中世纪城堡 × 轻蒸汽朋克。
- 风格：明亮、圆润、Q 版、清晰、色块化、中高明度、适中饱和度、轻度手绘、低细节噪声。
- 轻蒸汽朋克仅作点缀；单个物件通常只保留 1～2 个机械识别点。
- 背景低对比；水晶、当前关键反馈和唯一主按钮拥有最高局部亮度与最清晰边缘。
- 禁止阴暗恐怖、重金属、写实、赛博朋克、复杂机械堆砌、过量描边/外发光/阴影/粒子。
- 元素颜色固定：毒为黄绿/草绿，冰冻为青蓝/浅蓝，闪电为明黄/金黄，暴击为紫色，火焰为橙红，核心水晶为青蓝。
- 小图标必须在 96×96 下可辨认，优先单一主轮廓和 1～2 个辅助识别结构。

## UI 与切图硬规则

- 未确认效果图前，不切正式资源。
- 切图阶段不再进行美术重设计。
- 正式 UI 底板不得烘焙文字、数字、百分比、次数、动态角标或可替换图标；这些内容由程序绘制。
- 正式资源输出 PNG RGBA；可伸缩面板和按钮必须提供 NinePatch 安全边距与文本安全区。
- 同组状态必须使用相同画布尺寸、中心、锚点和基线，避免切换跳动。
- 默认保留 4～12 px 透明安全边距；禁止切薄轮廓。
- 透明边缘必须分别在白、浅灰、黑背景验收，不得有绿/紫杂边、白毛边、透明缺口、黑边断裂或背景残留。
- 优先从分层源文件导出；其次使用无字纯色底独立素材；只有无遮挡且边缘合格时才允许从确认图局部抠图。被文字或图标遮挡的区域不得硬裁成正式资源。
- 抠图底色选择主体不存在的互补色：绿色主体优先洋红 `#ff00ff`，紫色主体优先纯绿 `#00ff00`，蓝色高亮素材避免蓝幕。
- 裁切、重命名、排版和简单色彩调整使用本地工具，不为机械操作消耗生图额度。

## 目录、命名与版本

AI 原始产物：

`art/ai_generated/<tool>/<YYYY-MM-DD>/<HHMMSS>_<purpose>/`

生产中间文件：

`art/production/<module>/<YYYY-MM-DD>_<purpose>/{source,chroma,cutouts,previews,qa}/`

运行时资源按现有模块进入 `assets/runtime/`；不得另建同义目录。若项目已有更具体目录，以现有目录为准。

文件名统一使用小写英文和下划线或短横线，不使用中文和空格。推荐格式：

`<module>_<asset>_<state>_vNN.png`

版本从 `v01` 递增。不得覆盖来源不明或用户已有资源。

## 入库授权边界

“看效果”“出方案”“切图”只授权效果图或生产素材，不等于授权修改运行场景。

只有用户明确要求“替换到游戏、实现或入库”时，才可以：

1. 将资源复制到对应 `assets/runtime` 模块；
2. 更新场景、主题或脚本引用；
3. 设置 TextureRect、NinePatch、锚点和最小尺寸；
4. 等待 Godot 导入并检查 `.import`；
5. 执行 Godot 解析、主场景 Headless 和 Web 冒烟验证。

## 每次交付必须报告

- 任务名称、版本和实际完成内容；
- 效果图、无字生产图、透明切图的完整路径；
- 每个资源的像素尺寸、Alpha 状态、是否含字、是否含阴影/光效；
- NinePatch 边距、文本安全区和锚点；
- 白/灰/黑背景边缘 QA 结果；
- 是否已入库、是否已替换、是否完成运行验证；
- 使用的生成工具和最终提示词或提示词文件。

不得仅报告“已完成”。未验证的项目必须明确标为 `pending`，不能假定通过。
